# Set the “ea-php84” or “alt-php84” package as default PHP programming language
<IfModule mime_module>
  AddHandler application/x-httpd-ea-php84 .php .php8 .phtml
  AddHandler application/x-httpd-ea-php84___lsphp .php .php8 .phtml
  AddHandler application/x-httpd-alt-php84 .php .php8 .phtml
  AddHandler application/x-httpd-alt-php84___lsphp .php .php8 .phtml
  AddHandler application/x-httpd-ea-php83 .php .php8 .phtml
  AddHandler application/x-httpd-ea-php83___lsphp .php .php8 .phtml
</IfModule>

<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Direct access to diagnostic tools and static files
    RewriteCond %{REQUEST_URI} ^/(phpinfo\.php|favicon\.ico|favicon\.png|apple-touch-icon\.png|robots\.txt|sitemap\.xml)$ [NC]
    RewriteRule ^ - [L]

    # Security: Protect sensitive hidden files
    <FilesMatch "^\.env|\.git|\.cpanel\.yml|composer\.(json|lock)|package(-lock)?\.json">
        Order allow,deny
        Deny from all
    </FilesMatch>

    # Forward root and non-public requests to public directory
    RewriteRule ^$ public/index.php [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L]
</IfModule>
